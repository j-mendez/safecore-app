import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  row: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-around',
  },
  flex: {
    flex: 1,
  },
  input: {
    minHeight: 40,
    margin: 12,
  },
});

export {styles};
