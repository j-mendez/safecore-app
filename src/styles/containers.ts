import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  row: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-around',
  },
  container: {
    padding: 12,
  },
  flex: {
    flex: 1,
  },
  input: {
    minHeight: 40,
  },
});

export {styles};
